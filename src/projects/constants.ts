import { BASE_PREFIX } from '../common/base/constants';

export const BASE_PROJECTS_PATH = `${BASE_PREFIX}/projects`;

export const PROJECTS_PATH = {
	CREATE: '',
	GET_ALL_BY_USER_ID: '/user',
} as const;
